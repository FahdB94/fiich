# Copy this file to `.env.local` and fill in your Supabase project keys. These values are
# used by the Supabase client at runtime and must be present at build time.

NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# SMTP configuration for sending email invitations. Set these values if you want Fiich
# to email partners when you invite them. These values are used by nodemailer in
# app/api/send-invite/route.ts
SMTP_HOST=your-smtp-host
SMTP_PORT=587
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password
FROM_EMAIL=fiich@example.com